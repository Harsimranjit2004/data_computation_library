we have vector of type T we are reading a file line and then convert to specific template type 


1: std::is_same<T, int>::value return false or true if types are same this happens on the compile time 